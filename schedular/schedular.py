from .time_utils import parse_time, add_minute, overlaps


def get_free_slots():
    """Return list of available start times for a given working day, avoiding overlapping with existing bookings."""
    free_slots = []

    return free_slots
